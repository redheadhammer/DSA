
#include <stdio.h>
#include <string.h>

int add(char str1[], char str2[]){
    int len = strlen(str1);
}

int main(int argc, char *argv[]){
    int size = strlen(argv[1]);
    char str1[size];

    printf("size : %d", size);
    char str2[size];

}
